# 为什么使用 Superpowers

我们用 Codex 做开发时，它很容易直接开始写代码。

一个禅道任务丢给 Codex 后，如果没有约束，它可能会：

- 需求还没问清楚就开始实现。
- 不区分新页面、老需求优化、bug 修复。
- 多个需求并行时，把分支和工作区混在一起。
- 修 bug 时没有复现，直接猜原因。
- 改完代码后没有验证，就说已经完成。

`superpowers` 的作用，就是把 Codex 拉回工程流程里。

它主要解决这些问题：

- 需求不清时，先澄清。
- 多需求并行时，先隔离工作区。
- 改动较大时，先写计划。
- 修 bug 时，先复现和定位。
- 行为变化时，先用测试定义结果。
- 完成前，先验证。

## 1. using-superpowers：判断工作流

**用来做什么：**
让 Codex 在开始处理任务前，先判断当前任务应该进入哪种工作流。

**解决什么问题：**
避免 Codex 拿到禅道任务后直接开写，导致需求没澄清、流程选错、改动范围失控。

**常用 prompt：**

```text
这是一个禅道任务。请先使用 using-superpowers，判断这个任务应该进入哪种工作流，再开始处理。
```

`using-superpowers` 本身不负责完成需求，它负责帮 Codex 选对下一步。

比如：

- 需求不清楚，下一步进入 `brainstorming`。
- 需要独立分支，下一步进入 `using-git-worktrees`。
- 改动比较大，下一步进入 `writing-plans`。
- 是 bug，下一步进入 `systematic-debugging`。
- 行为变化明确，下一步进入 `test-driven-development`。
- 准备收尾，下一步进入 `verification-before-completion`。

## 2. brainstorming：澄清需求

**用来做什么：**
当禅道任务还不够清楚时，先让 Codex 帮我们澄清需求，再进入实现。

**解决什么问题：**
很多任务标题看起来简单，但实际可能有多种理解。如果不先澄清，Codex 很容易按自己的理解直接写，最后做出来的不是我们真正要的结果。

比如：

```text
优化订单详情页金额展示逻辑
```

这个任务可能是在说：

- 页面样式要调整。
- 金额字段要换展示规则。
- 接口返回字段变了。
- 历史金额计算逻辑有 bug。
- 导出、打印、弹窗也要同步修改。

这时就适合使用 `brainstorming`。

它和 Codex `plan` 的区别：

| 对比 | `brainstorming` | Codex `plan` |
| --- | --- | --- |
| 关注点 | 需求是否清楚 | 实现步骤是否清楚 |
| 解决的问题 | “做什么”还不明确 | “怎么做”需要拆解 |
| 适用时机 | 禅道任务刚拿到、描述模糊 | 需求已经明确，准备实现 |
| 产出 | 需求理解、边界、验收点、方案取舍 | 步骤列表、文件范围、验证方式 |
| 核心价值 | 防止做错方向 | 防止实现过程失控 |

一句话理解：

> `brainstorming` 先确认方向，Codex `plan` 再拆执行路径。方向没确认前，不要急着 plan。

**常用 prompt：**

```text
这是一个禅道任务。请使用 brainstorming，先帮我澄清需求范围、边界和验收点，再开始实现。
```

`brainstorming` 应该帮我们确认：

- 这个任务真正要解决什么问题。
- 哪些行为需要改变。
- 哪些已有行为不能变。
- 影响哪些页面、接口或组件。
- 最小可交付范围是什么。
- 怎么判断这个任务完成了。

## 3. writing-plans：拆实现计划

**用来做什么：**
在开始修改代码前，先让 Codex 把实现步骤写清楚。

**如何理解：**
`writing-plans` 可以类比成一份轻量 spec。它不是产品需求文档，也不是完整技术设计文档，而是实现前的执行说明：这次改什么、怎么改、涉及哪些文件、怎么验证。

**解决什么问题：**
需求澄清之后，如果直接让 Codex 开始改，它可能会一边理解一边改代码，导致改动范围越来越大，也不方便 review。

尤其适合这些任务：

- 新页面。
- 老页面优化。
- 涉及多个组件或接口的改动。
- 需要保持历史行为不变的需求。

**常用 prompt：**

```text
这是一个禅道任务。请使用 writing-plans，先列出实现步骤、涉及文件和验证方式，再开始修改代码。
```

`writing-plans` 应该帮我们确认：

- 需要修改哪些文件。
- 每一步具体做什么。
- 哪些已有行为不能变。
- 需要补哪些测试或验证。
- 完成后怎么确认任务可交付。

## 4. using-git-worktrees：隔离工作区

**用来做什么：**
为一个禅道任务准备独立工作区和分支，再开始修改代码。

**解决什么问题：**
多个任务并行开发时，如果都在同一个目录里切来切去，很容易出现未提交改动互相影响、分支切换困难、回滚不清晰的问题。

尤其适合这些情况：

- 同时处理多个禅道任务。
- 当前分支上已经有未完成改动。
- 临时插入一个优先级更高的需求或 bug。
- 一个任务准备合并到 `dev`，另一个任务还不能合并。

**常用 prompt：**

```text
这是一个禅道任务。请使用 using-git-worktrees，为这个任务创建独立工作区和分支，再开始处理。
```

`using-git-worktrees` 应该帮我们确认：

- 当前是否已经在独立工作区。
- 是否需要新建 worktree 和分支。
- 新工作区是否安装好依赖。
- 开始改动前，当前工作区是否干净。

## 5. systematic-debugging：系统化调试

**用来做什么：**
修 bug 时，要求 Codex 按“复现问题 -> 定位原因 -> 最小修复 -> 验证结果”的顺序处理。

**解决什么问题：**
避免 Codex 没有复现问题就直接猜原因，改完之后也不知道是不是真的修好了。

尤其适合这些情况：

- 禅道任务是 bug。
- 测试环境或线上反馈了异常现象。
- 老需求优化后出现回归。
- 问题只在某些边界条件下出现。

**常用 prompt：**

```text
这是一个禅道 bug 任务。请使用 systematic-debugging，先复现问题，再定位原因，做最小修复并验证。
```

`systematic-debugging` 应该帮我们确认：

- 问题是否能复现。
- 复现步骤是什么。
- 失败现象和预期行为分别是什么。
- 根因在哪里。
- 修复是否足够小。
- 修复后是否重新验证。

## 6. test-driven-development：测试驱动开发

**用来做什么：**
在修改行为前，先写一个失败测试，再写最小实现让测试通过。

**解决什么问题：**
避免 Codex 先改代码、后补测试，导致测试只是验证“现在的实现”，而不是验证“这次需求真正要求的行为”。

尤其适合这些情况：

- 新增业务规则。
- 修改已有判断逻辑。
- 修复可以自动化复现的 bug。
- 调整工具函数、状态计算、边界逻辑。

**常用 prompt：**

```text
这是一个行为变更任务。请使用 test-driven-development，先写失败测试，再实现最小改动并验证。
```

`test-driven-development` 应该帮我们确认：

- 先写的测试是否会失败。
- 失败原因是否和需求相关。
- 实现是否只做了最小改动。
- 修改后测试是否通过。
- 是否影响已有测试。

TDD 不是为了多写测试，而是为了让 Codex 先用测试定义“什么叫做改对了”。

## 7. verification-before-completion：完成前验证

**用来做什么：**
在 Codex 声称完成前，先运行相关检查，并明确说明哪些通过、哪些失败。

**解决什么问题：**
避免 Codex 只说“应该可以了”或“已经修好了”，但没有测试、构建、lint、页面验证等证据。

尤其适合这些情况：

- 准备提交代码。
- 准备提测。
- 准备合并到 `dev` 或 `master`。
- Codex 已经说某个问题修好了。

**常用 prompt：**

```text
请使用 verification-before-completion，运行相关检查，并明确说明哪些通过、哪些失败。
```

`verification-before-completion` 应该帮我们确认：

- 单元测试是否通过。
- 类型检查是否通过。
- lint 是否通过。
- 构建是否通过。
- 关键页面或关键流程是否验证。
- 禅道验收点是否逐条对照。

完成不是“看起来改好了”，而是“有命令、有结果、有证据”。

## 补充经验

这些不是新的主流程，而是日常使用 Codex 时可以配合使用的操作习惯。

### 1. 上下文不要越堆越乱

一个会话尽量只处理一个任务。如果同一个会话里既聊需求、又排错、又重构、又发版，Codex 很容易把旧约束、失败尝试和当前目标混在一起。

建议这样处理：

1. 一个禅道任务对应一个会话。
2. 如果任务很长，中途让 Codex 生成 handoff。
3. handoff 里只保留当前继续工作需要的信息。
4. 开新会话时，只贴 handoff、相关文件、失败日志和验收命令。

可以直接使用这个 prompt：

```text
请为当前任务生成一份 handoff，用于开启新会话继续处理。

要求包含：
1. 当前任务目标
2. 已经修改的文件
3. 已经确认的结论
4. 已经运行过的命令和结果
5. 当前还没解决的问题
6. 下一个会话需要优先阅读的文件
7. 下一步建议操作

不要复述无关聊天过程，只保留继续完成任务需要的信息。
```

handoff 可以按这个结构写：

```md
## 当前任务

简要说明这个禅道任务要解决什么。

## 已修改文件

- `path/to/file`
- `path/to/another-file`

## 已确认结论

- 哪些方案已经确认可行
- 哪些方向已经排除

## 已运行命令

- `npm test`：失败，失败用例是 xxx
- `mvn test`：未运行，原因是 xxx

## 未解决问题

- 还差哪个边界场景
- 哪个测试还没通过

## 下个会话先读

- `path/to/spec`
- `path/to/component`
- `path/to/test`

## 下一步

1. 先复现 xxx
2. 再修改 xxx
3. 最后运行 xxx 验证
```

### 2. AGENTS.md 写长期有效的工程约定

`AGENTS.md` 是给 Codex 的项目级规则，不是第二份 README。它应该记录长期有效、必须遵守、代码里不一定能读出来的工程约定。

可以参考 `andrej-karpathy-skills` 这类规则，但不要整份照抄。它的价值在于提醒我们约束 Codex 的工程行为，比如先澄清、保持简单、精准改动、以验证为完成标准。

建议这样写：

1. 先写通用工程原则。
2. 再写当前项目特有约定。
3. 写清常用验证命令。
4. 写清禁止修改的目录或文件。
5. 写清高风险操作，比如数据库迁移、生产配置、密钥文件。

判断一条规则要不要写进去，可以问一句：

> 这行删掉后，Codex 会不会更容易犯错？

如果不会，就不要写。

可以先用这份简化版作为起点：

```md
## Codex 工作约定

### 1. 先澄清再实现

如果禅道任务有多种理解，先说明不确定点并提问，不要直接选择一种解释实现。

### 2. 保持简单

只实现当前任务需要的内容。不要添加未要求的配置、扩展点、抽象层或兼容逻辑。

### 3. 精准改动

只修改和当前禅道任务直接相关的文件。不要顺手重构、格式化或删除无关代码。

### 4. 以验证为完成标准

每个任务都要说明验证方式。修 bug 要能复现，行为变更要有测试或明确验收步骤。

## 项目规则

- 修改 `src/services/**` 后必须运行对应单元测试。
- 不要修改 `legacy-api/**` 的响应结构，它仍被老客户端使用。
- 不要读取或修改 `.env.production`、`secrets/**`。
- 前端页面必须使用现有组件库，不新增 UI 库。
- 提交前运行 `npm test` 和 `npm run lint`。
```

### 3. 给 Codex 看项目里的金标准范例

不要只说“按最佳实践实现”。这个范围太宽，Codex 可能写出看起来不错、但和当前项目风格不一致的代码。

建议这样做：

1. 找一个项目里已经写得比较好的同类文件。
2. 让 Codex 先阅读这些文件。
3. 明确要求复用相同的分层、返回结构、异常处理、日志风格和测试写法。
4. 明确禁止引入新的风格或新的基础设施。

后端任务可以这样写：

```text
先阅读 UserController、UserService、UserRepository 和对应测试。
参考它们的分层方式、异常处理、返回体包装、日志风格和测试写法。
然后实现 OrderExportController。
不要引入新的响应格式。
不要新增全局异常处理器。
不要绕过现有权限校验逻辑。
```

前端任务可以这样写：

```text
先阅读 src/pages/UserList.vue 和 src/components/SearchForm.vue。
参考它们的筛选区、表格布局、分页处理和接口调用方式。
然后实现订单列表页面。
使用现有组件库，不新增 UI 库。
页面是后台工具，信息密度优先，不要做成营销页风格。
```

### 4. 完成看证据，不看总结

Codex 的总结只能参考，不能当作完成依据。真正要看的是 diff、测试结果、构建结果、lint 结果和关键页面验证结果。

建议每次收尾都做这几步：

1. 看影响范围：`git diff --stat`
2. 看关键文件：`git diff`
3. 跑验证命令：测试、构建、lint
4. 如果是前端页面，至少打开页面做一次关键流程验证
5. 让 Codex 明确写出哪些检查通过，哪些没有运行

可以直接使用这个 prompt：

```text
请不要只总结“已完成”。

请按下面格式汇报：
1. 修改了哪些文件
2. 关键 diff 是什么
3. 运行了哪些验证命令
4. 每条命令的结果是什么
5. 哪些检查没有运行，原因是什么
6. 还有哪些风险或待人工确认点
```

如果没有运行验证，就让 Codex 写“未运行”和原因，不要让它用“应该可以”带过。

### 5. 每完成一个 Task，就测试、看 diff、小步提交

一个禅道任务可以拆成多个小 Task，不要让 Codex 一口气从头做到尾。每完成一个 Task，就停下来验证一次。

这样做是为了解决这些问题：

- Codex 一次性改太多文件，review 成本很高。
- 中途方向错了，不知道从哪里回滚。
- 测试最后才跑，多个问题堆在一起，很难定位。
- 一个 commit 里混了多个意图，后续合并和排查都困难。
- Codex 总结说得很顺，但实际 diff 可能已经超出任务范围。

建议这样做：

1. 先让 Codex 根据计划拆出多个 Task。
2. 每次只执行一个 Task。
3. 当前 Task 完成后，先运行相关测试或验证命令。
4. 再看 `git diff --stat` 和关键文件的 `git diff`。
5. 确认没有问题后再提交。
6. 一个 commit 只表达一个清晰意图。

可以这样要求 Codex：

```text
请把这个禅道任务拆成多个小 Task。
每次只执行一个 Task。
每完成一个 Task 后，先停下来：
1. 运行相关测试或验证命令
2. 展示 git diff --stat
3. 总结关键 diff
4. 等我确认后再继续下一个 Task
```

提交时可以这样约束：

```text
请为当前已完成的 Task 准备一次小步提交。
要求：
1. 只包含当前 Task 相关改动
2. 不混入其他需求
3. 提交前说明验证命令和结果
4. commit message 简洁描述这一步做了什么
```

小步提交不是为了多几个 commit，而是为了让每一步都能 review、验证和回滚。
